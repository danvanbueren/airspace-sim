/** Delay after the last resize event before treating resize as complete. */
export const WORKSPACE_RESIZE_IDLE_MS = 150

/** Throttled layout refresh rate while the workspace is actively resizing. */
export const WORKSPACE_RESIZE_LAYOUT_THROTTLE_MS = 100
